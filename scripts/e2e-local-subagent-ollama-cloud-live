#!/usr/bin/env bash
set -euo pipefail

cd "$(dirname "$0")/.."

if [ -f .env.d/ollama.env ]; then
  set -a
  # shellcheck disable=SC1091
  . .env.d/ollama.env
  set +a
fi

exec venv/bin/python -m tests.e2e.local_subagent_ollama_cloud_live "$@"
